<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="in.koala.mapper.UserMapper">
    <select id="test" resultType="string">
        SELECT id from user limit 1;
    </select>
    <select id="findId" resultType="string">
        SELECT account FROM user WHERE account = #{account};
    </select>
    <insert id="signUp" parameterType="in.koala.domain.naverLogin.NaverUser">
        INSERT INTO user (account, nickname, profile, sns_email)
        VALUES(#{id}, #{nickname}, #{profile_image}, #{email})
    </insert>
</mapper>