<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="in.koala.mapper.KeywordPushMapper">

    <select id="pushKeyword" parameterType="java.lang.Long" resultType="java.util.Map">
        SELECT c.title, c.url
        FROM crawling as c
                 INNER JOIN keyword as k ON c.title LIKE CONCAT('%',k.keyword,'%')
                 INNER JOIN user_keyword as uk ON uk.keyword_id = k.id
        WHERE uk.user_id = #{userId};
    </select>
</mapper>